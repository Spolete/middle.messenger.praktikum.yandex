<div class="{{styles.profile}}">
    <a href="/profile" class="{{styles.back}}">Назад</a>
    {{{ avatar }}}
    <div class="{{styles.profile-info}}">
        {{{ rowChangePrev }}}
        {{{ rowChangeNew }}}
        {{{ rowChangeNewSecond }}}
    </div>
    <div class="{{styles.profile-menu}}">
        <a>{{{ button }}}</a>
    </div>
</div>

