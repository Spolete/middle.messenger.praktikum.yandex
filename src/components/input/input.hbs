<div class="input">
    <input class="input__field" placeholder="{{ placeholder }}" type="{{ type }}" name="{{ name }}" id="{{ id }}"/>
    <span class="input__error">{{ error }}</span>
    <label class="input__label" for="{{ name }}">{{ placeholder }}</label>
</div>